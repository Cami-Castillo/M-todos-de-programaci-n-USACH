#include <stdio.h>
#include <stdlib.h>

//Camila Castillo
// NOTA:3 (no recuerdo ni el enunciado y a todos nos fue mal)


int main (){

	int arreglo[10];
	int i;

	int valor=0;
	for (int i=0; i<10; ++i){

		printf("Ingrese los elementos: %i", i++);
		scanf("%d", &valor);
		arreglo[i]=valor;
	}

}
	
	int tamaño=0

	for (int i=0; i<10; i++){

		if ((arreglo[i]%arreglo[i]==0) &&  (arreglo[i]%1==0));
			tamaño=tamaño+1;   


	int *arreglo=(int*)malloc(sizeof(int)*tamaño);



	

	free(arreglo2);

	return 0 
}